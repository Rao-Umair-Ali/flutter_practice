void main() {
  //----------------------------------------Question number 5--------------------------------------------------

/*Write a program create two integer variables 'a' and 'b' and assign them any number and then check if both the conditions 
'a < 50' and 'a < b' are true. Display the result, now check if at-least one of the conditions 'a < 50' and 'a < b' is true.*/
  int a = 13;
  int b = 7;
  print(a < 50 && a < b);
  print(a < 50 || a < b);
}
